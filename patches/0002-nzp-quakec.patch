diff --git a/progs/csqc.src b/progs/csqc.src
index d1ee0b0..28bfcaf 100644
--- a/progs/csqc.src
+++ b/progs/csqc.src
@@ -33,5 +33,6 @@ view_model.qc
 particles.qc
 chasecam.qc
 zombie.qc
+touch.qc
 main.qc
 #endlist
diff --git a/source/client/hud.qc b/source/client/hud.qc
index 44cd843..4212be4 100644
--- a/source/client/hud.qc
+++ b/source/client/hud.qc
@@ -1049,11 +1049,11 @@ void() HUD_Useprint =
     if (usebutton == "")
         usebutton = "UNBOUND";
 
-	// If this is a gamepad button, the space we want to reserve
-	// in the usestring should be a fixed width.
-	if (Key_IsControllerGlyph(usebutton)) {
+	// If this is a gamepad button (or we're on touch, where we draw an icon),
+	// the space we want to reserve in the usestring should be a fixed width.
+	if (Key_IsControllerGlyph(usebutton) || !last_input_was_gamepad) {
 		usespace = " ";
-	} 
+	}
 	// Scale the space in the usestring for the bind according to
 	// the bind's name.
 	else {
@@ -1169,7 +1169,9 @@ void() HUD_Useprint =
 	if (substring(usestring, 0, 4) == "Hold") {
 		button_width = x + getTextWidth("Hold ", 12);
 
-		if (Key_IsControllerGlyph(usebutton))
+		if (!last_input_was_gamepad)	// touch: show the on-screen use button, not a key
+			drawpic([button_width - 4, g_height/2 + 59, 0], "gfx/touch/btn_activate", [20, 20], [1, 1, 0], 1);
+		else if (Key_IsControllerGlyph(usebutton))
 			Key_DrawControllerGlyph([button_width - 5, g_height/2 + 62], usebutton, [18, 18]);
 		else
 			Draw_String([button_width, g_height/2 + 65, 0], usebutton, [12, 12, 0], [1, 1, 0], 1, 0);
diff --git a/source/client/main.qc b/source/client/main.qc
index 47aea3b..5d6c148 100644
--- a/source/client/main.qc
+++ b/source/client/main.qc
@@ -799,7 +799,9 @@ void(vector client_view_angles) Camera_AimSnap =
 {
     float zoom_value = getstatf(STAT_WEAPONZOOM);
 
-	if (!(gamepad_enabled && cvar("in_aimassist") == 1))
+	// controllers get aim assist only when they've enabled it; touch always gets it
+	// (mobile leans on it much harder, like CoD-mobile). controller path is unchanged.
+	if (last_input_was_gamepad && !(gamepad_enabled && cvar("in_aimassist") == 1))
 		return;
 
     if (zoom_value == 1 || zoom_value == 2) {
@@ -997,6 +999,7 @@ noref void(float width, float height, float menushown) CSQC_UpdateView =
 	{
 		setcursormode(FALSE);
 		HUD_DrawElements();
+		Touch_DrawControls();
 		Chat_Draw();
 		setlocaluserinfo(0, "in_menu", "0");
 	}
@@ -1060,11 +1063,37 @@ void(float scanx, float setval) Input_Movecheck =
     }
 }
 
+// Controller B = back one level for the in-game menus (pause + its submenus).
+// These run in CSQC (not menu.dat), and use #else back-targets (MENU_PAUSE),
+// so they need their own back logic separate from menu/main.qc's Menu_GoBack.
+void() CSQC_MenuBack =
+{
+	Menu_PlaySound(MENU_SND_ENTER);
+	if (current_menu == MENU_VIDEO || current_menu == MENU_AUDIO || current_menu == MENU_CONTROL || current_menu == MENU_ACCESSIBILITY)
+		current_menu = MENU_OPTIONS;
+	else if (current_menu == MENU_GAMEPAD || current_menu == MENU_BINDINGS)
+		current_menu = MENU_CONTROL;
+	else if (current_menu == MENU_OPTIONS)
+		current_menu = MENU_PAUSE;
+	else if (current_menu == MENU_PAUSE && menu_paus_submenu != 0)
+		menu_paus_submenu = 0;			// back out of a confirm dialog
+	else
+		ToggleMenu();					// resume the game
+};
+
 noref float(float evtype, float scanx, float chary, float devid) CSQC_InputEvent =
 {
 	if (evtype == IE_GYROSCOPE || evtype == IE_ACCELEROMETER)
 		return FALSE;
 
+	// on-screen touch controls (in-game only). when it claims the event we also
+	// flag the input as non-gamepad so the controller path stays out of the way.
+	if (Touch_InputEvent(evtype, scanx, chary, devid)) {
+		last_input_was_gamepad = FALSE;
+		setlocaluserinfo(0, "using_gamepad", "0");
+		return TRUE;
+	}
+
 	// Ignore subtle (drift-y) joystick axes
 	if (evtype == IE_JOYAXIS && devid > 0)
 	{
@@ -1094,6 +1123,7 @@ noref float(float evtype, float scanx, float chary, float devid) CSQC_InputEvent
 	if (last_input_deviceid > 0)
 	{
 		last_input_was_gamepad = TRUE;
+		gamepad_enabled = TRUE;	// once a controller is used, enable gamepad features (aim assist). Never set anywhere upstream, so aim assist was dead.
 		setlocaluserinfo(0, "using_gamepad", "1");
 	}
 	else
@@ -1104,6 +1134,11 @@ noref float(float evtype, float scanx, float chary, float devid) CSQC_InputEvent
 
 	if (current_menu != MENU_NONE)
 	{
+		if (evtype == IE_KEYDOWN && scanx == K_GP_B && _sui_binding_command == "")
+		{
+			CSQC_MenuBack();
+			return TRUE;
+		}
 		return sui_input_event(evtype, scanx, chary, devid);
 	}
 
@@ -1191,6 +1226,8 @@ noref float(float evtype, float scanx, float chary, float devid) CSQC_InputEvent
 
 noref void() CSQC_Input_Frame =
 {
+	Touch_Frame();
+
 	input_angles += gun_kick;
 	input_angles += sniper_sway;
 	input_angles += cam_snap_difference;
diff --git a/source/client/touch.qc b/source/client/touch.qc
new file mode 100644
index 0000000..38e6b0a
--- /dev/null
+++ b/source/client/touch.qc
@@ -0,0 +1,385 @@
+/*
+	touch.qc -- on-screen touch controls
+
+	The action-button artwork (gfx/touch/btn_*) is from glKarin's Diii4a /
+	idTech4A++ Android engine (github.com/glKarin/com.n0n3m4.diii4a), GPL-3.0,
+	same license family as this tree. The movement-stick ring/knob are ours.
+
+	Layout: a floating movement stick under the left thumb, look-drag under the
+	right thumb, and a column of action buttons down the right edge. Everything
+	here only runs while actually in a game -- menus and the pause screen are
+	left completely alone, and the controller path is never touched.
+*/
+
+// touch keycodes as the engine reports them to CSQC (keys.h enum; confirmed on-device)
+#define K_TOUCH       600
+#define K_TOUCHSLIDE  601
+#define K_TOUCHTAP    602
+#define K_TOUCHLONG   603
+
+// action buttons -- ADS-fire is the one and only shoot button now
+#define TB_ADSFIRE  0	// hold to aim down sights, then fire (CoD-mobile style)
+#define TB_RELOAD   1
+#define TB_KNIFE    2
+#define TB_GRENADE  3
+#define TB_USE      4
+#define TB_SWAP     5
+#define TB_JUMP     6
+#define TB_COUNT    7
+
+// per-finger roles
+#define TR_NONE  0
+#define TR_MOVE  1
+#define TR_LOOK  2
+#define TR_BTN   3
+
+#define TOUCH_MAXFINGERS 10
+
+float touch_active[TOUCH_MAXFINGERS];
+float touch_role[TOUCH_MAXFINGERS];
+float touch_curx[TOUCH_MAXFINGERS];
+float touch_cury[TOUCH_MAXFINGERS];
+float touch_orgx[TOUCH_MAXFINGERS];	// stick origin, or look last-pos
+float touch_orgy[TOUCH_MAXFINGERS];
+float touch_btn[TOUCH_MAXFINGERS];
+
+// movement + look handed to CSQC_Input_Frame
+float touch_move_on;
+float touch_move_fwd, touch_move_side;
+float touch_look_dyaw, touch_look_dpitch;
+
+// fire / aim resolution
+float touch_ads_was;		// was the ADS-fire button held last frame
+float touch_ads_start;		// cltime the ADS-fire press began (for the aim-then-fire delay)
+float touch_sniper_armed;	// scoped sniper held -- fire on release ("quickscope")
+float touch_sniper_fire_until;	// hold the shot briefly after a sniper release
+
+
+// geometry of action button 'id' -> [centre_x, centre_y, size]
+vector(float id) Touch_BtnGeom =
+{
+	float w = g_width, h = g_height, b = h * 0.13;
+	// two tight columns down the right edge so the centre-right stays open for look
+	switch (id) {
+		case TB_ADSFIRE: return [w * 0.87, h * 0.83, b * 1.35];	// main shoot button
+		case TB_RELOAD:  return [w * 0.75, h * 0.87, b * 0.95];
+		case TB_KNIFE:   return [w * 0.92, h * 0.58, b * 0.90];
+		case TB_USE:     return [w * 0.79, h * 0.58, b * 0.90];
+		case TB_GRENADE: return [w * 0.92, h * 0.38, b * 0.90];
+		case TB_JUMP:    return [w * 0.79, h * 0.38, b * 0.90];
+		case TB_SWAP:    return [w * 0.92, h * 0.20, b * 0.85];
+	}
+	return '0 0 0';
+};
+
+string(float id) Touch_BtnPic =
+{
+	switch (id) {
+		case TB_ADSFIRE: return "gfx/touch/touch_adsfire";	// aim + fire
+		case TB_RELOAD:  return "gfx/touch/btn_reload";
+		case TB_KNIFE:   return "gfx/touch/touch_knife";
+		case TB_GRENADE: return "gfx/touch/touch_grenade";
+		case TB_USE:     return "gfx/touch/btn_activate";	// hand = use / buy
+		case TB_SWAP:    return "gfx/touch/touch_swap";
+		case TB_JUMP:    return "gfx/touch/btn_jump";
+	}
+	return "";
+};
+
+void(float id) Touch_BtnPress =
+{
+	switch (id) {
+		case TB_ADSFIRE: break;	// shooting is resolved in Touch_Frame
+		case TB_RELOAD:  localcmd("+button5\n");   break;
+		case TB_KNIFE:   localcmd("+button6\n");   break;
+		case TB_GRENADE: localcmd("+button3\n");   break;
+		case TB_USE:     localcmd("+button7\n");   break;
+		case TB_SWAP:    localcmd("+button4\n");   break;
+		case TB_JUMP:    localcmd("impulse 10\n"); break;
+	}
+};
+
+void(float id) Touch_BtnRelease =
+{
+	switch (id) {
+		case TB_RELOAD:  localcmd("-button5\n");  break;
+		case TB_KNIFE:   localcmd("-button6\n");  break;
+		case TB_GRENADE: localcmd("-button3\n");  break;
+		case TB_USE:     localcmd("-button7\n");  break;
+		case TB_SWAP:    localcmd("-button4\n");  break;
+		// FIRE / ADSFIRE / AIM / JUMP: nothing to release here
+	}
+};
+
+// which button (if any) sits under (x, y); -1 for none. hit area padded slightly.
+float(float x, float y) Touch_HitButton =
+{
+	float i;
+	for (i = 0; i < TB_COUNT; i = i + 1) {
+		vector bg = Touch_BtnGeom(i);
+		float hs = bg_z * 0.5 * 1.15;
+		if (x >= bg_x - hs && x <= bg_x + hs && y >= bg_y - hs && y <= bg_y + hs)
+			return i;
+	}
+	return -1;
+};
+
+// is action 'id' currently held by a finger? (for the press highlight)
+float(float id) Touch_BtnHeld =
+{
+	float i;
+	for (i = 0; i < TOUCH_MAXFINGERS; i = i + 1)
+		if (touch_active[i] && touch_role[i] == TR_BTN && touch_btn[i] == id)
+			return TRUE;
+	return FALSE;
+};
+
+void(float devid) Touch_FingerUp =
+{
+	if (!touch_active[devid])
+		return;
+	if (touch_role[devid] == TR_BTN)
+		Touch_BtnRelease(touch_btn[devid]);
+	else if (touch_role[devid] == TR_MOVE) {
+		touch_move_on = FALSE;
+		touch_move_fwd = 0;
+		touch_move_side = 0;
+	}
+	touch_active[devid] = FALSE;
+	touch_role[devid] = TR_NONE;
+};
+
+void(float devid, float x, float y) Touch_FingerMove =
+{
+	touch_curx[devid] = x;
+	touch_cury[devid] = y;
+
+	if (touch_role[devid] == TR_NONE) {
+		// first sample after touch-down decides what this finger is for
+		float bid = Touch_HitButton(x, y);
+		if (bid >= 0) {
+			touch_role[devid] = TR_BTN;
+			touch_btn[devid] = bid;
+			touch_orgx[devid] = x;	// so fire/ads-fire can double as a look pad
+			touch_orgy[devid] = y;
+			Touch_BtnPress(bid);
+			return;
+		} else if (x < g_width * 0.42) {
+			touch_role[devid] = TR_MOVE;
+			touch_move_on = TRUE;
+			// fall through and engage straight away
+		} else {
+			touch_role[devid] = TR_LOOK;
+			touch_orgx[devid] = x;
+			touch_orgy[devid] = y;
+			return;
+		}
+	}
+
+	if (touch_role[devid] == TR_MOVE) {
+		// fixed (locked) stick centred at the bottom-left; the knob clamps to the rim
+		float scx = g_width * 0.15, scy = g_height * 0.74, radius = g_height * 0.16;
+		float dx = x - scx, dy = y - scy;
+		float mag = sqrt(dx * dx + dy * dy);
+		if (mag > radius && mag > 0) { dx = dx * (radius / mag); dy = dy * (radius / mag); }
+		touch_curx[devid] = scx + dx;	// clamped knob position, for drawing
+		touch_cury[devid] = scy + dy;
+		float fspeed = cvar("cl_forwardspeed"); if (fspeed <= 0) fspeed = 400;
+		float sspeed = cvar("cl_sidespeed");    if (sspeed <= 0) sspeed = 400;
+		touch_move_fwd  = (-dy / radius) * fspeed;	// up on screen = forward
+		touch_move_side = ( dx / radius) * sspeed;
+	}
+	else if (touch_role[devid] == TR_LOOK) {
+		float sens = cvar("touch_looksens"); if (sens <= 0) sens = 0.35;
+		float lx = x - touch_orgx[devid];
+		float ly = y - touch_orgy[devid];
+		touch_look_dyaw   = touch_look_dyaw   - lx * sens;	// drag right = look right
+		touch_look_dpitch = touch_look_dpitch + ly * sens;	// drag down = look down (+x=down)
+		touch_orgx[devid] = x;
+		touch_orgy[devid] = y;
+	}
+	else if (touch_role[devid] == TR_BTN) {
+		// the shoot button doubles as a look pad: drag off it to aim while firing
+		if (touch_btn[devid] == TB_ADSFIRE) {
+			float bsens = cvar("touch_looksens"); if (bsens <= 0) bsens = 0.35;
+			touch_look_dyaw   = touch_look_dyaw   - (x - touch_orgx[devid]) * bsens;
+			touch_look_dpitch = touch_look_dpitch + (y - touch_orgy[devid]) * bsens;
+			touch_orgx[devid] = x;
+			touch_orgy[devid] = y;
+		}
+	}
+};
+
+// returns TRUE when it handled (and so consumes) the event
+float(float evtype, float scanx, float chary, float devid) Touch_InputEvent =
+{
+	if (current_menu != MENU_NONE)
+		return FALSE;
+	if (devid < 0 || devid >= TOUCH_MAXFINGERS)
+		return FALSE;
+
+	if (evtype == IE_KEYDOWN && scanx == K_TOUCH) {
+		touch_active[devid] = TRUE;
+		touch_role[devid] = TR_NONE;	// assigned on the first position sample
+		return TRUE;
+	}
+	if (evtype == IE_KEYUP && scanx == K_TOUCH) {
+		Touch_FingerUp(devid);
+		return TRUE;
+	}
+	if ((evtype == IE_KEYDOWN || evtype == IE_KEYUP) &&
+	    (scanx == K_TOUCHSLIDE || scanx == K_TOUCHTAP || scanx == K_TOUCHLONG))
+		return TRUE;	// swallow the slide/tap companions
+
+	if (evtype == IE_MOUSEABS) {
+		// only track fingers that are genuinely down. the engine emits a trailing
+		// position sample *after* the touch-up; honouring that would re-engage a
+		// finger that was just released (stuck stick / look).
+		if (touch_active[devid])
+			Touch_FingerMove(devid, scanx, chary);
+		return TRUE;
+	}
+	if (evtype == IE_MOUSEDELTA)
+		return TRUE;
+
+	return FALSE;
+};
+
+// scoped bolt-action snipers -- aim, then fire on release, and never fire mid-scope
+float(float wep) Touch_IsSniper =
+{
+	float base = EqualNonPapWeapon(wep);
+	return (base == W_KAR_SCOPE || base == W_PTRS || wep == W_HEADCRACKER);
+};
+
+// fold movement + look into this frame's command. called first in CSQC_Input_Frame
+// so weapon kick/sway still layer on top. left untouched while a controller is in use.
+void() Touch_Frame =
+{
+	if (last_input_was_gamepad)
+		return;	// controller owns input -- stay completely out of the way
+
+	// guard against a dropped touch-up leaving the player walking by itself
+	float fi, moving = FALSE;
+	for (fi = 0; fi < TOUCH_MAXFINGERS; fi = fi + 1)
+		if (touch_active[fi] && touch_role[fi] == TR_MOVE) { moving = TRUE; break; }
+	if (!moving) { touch_move_on = FALSE; touch_move_fwd = 0; touch_move_side = 0; }
+
+	// --- shooting: the ADS-fire button is the only shoot button now ---
+	float bi, adsfire_held = FALSE;
+	for (bi = 0; bi < TOUCH_MAXFINGERS; bi = bi + 1)
+		if (touch_active[bi] && touch_role[bi] == TR_BTN && touch_btn[bi] == TB_ADSFIRE)
+			adsfire_held = TRUE;
+
+	if (adsfire_held && !touch_ads_was)
+		touch_ads_start = cltime;
+
+	float wz = getstatf(STAT_WEAPONZOOM);	// 0 = hip, 1 = ADS, 2 = scoped sniper
+	float actwep = getstatf(STAT_ACTIVEWEAPON);
+	float firetype = GetFiretype(EqualNonPapWeapon(actwep));
+
+	// fire behaviour by weapon:
+	//   release-fire (aim, shoot on let-go): scoped snipers + rocket launchers
+	//   hold (continuous):                   full-autos, tesla, flamethrower
+	//   tap  (one shot per deliberate press): semi, grenade, blade, ray, everything else
+	float is_release = Touch_IsSniper(actwep) || firetype == FIRETYPE_ROCKET;
+	float is_hold    = (firetype == FIRETYPE_FULLAUTO || firetype == FIRETYPE_TESLA || firetype == FIRETYPE_FLAME);
+
+	// weapons with sights wait a beat so they don't fire mid-raise; weapons without
+	// (tesla, ray, ballistic knife, flamethrower...) fire the instant you press
+	float weapon_adses = (firetype == FIRETYPE_SEMIAUTO || firetype == FIRETYPE_FULLAUTO || is_release);
+	float settle = weapon_adses ? 0.12 : 0;
+	float fire_ok = adsfire_held && (cltime - touch_ads_start) >= settle;
+
+	if (adsfire_held)
+		input_buttons = input_buttons | 128;	// hold ADS (a no-op for non-sighted weapons)
+
+	if (is_release) {
+		if (adsfire_held)
+			touch_sniper_armed = TRUE;	// never fire while held -- wait for the release
+	} else if (fire_ok) {
+		if (is_hold) {
+			input_buttons = input_buttons | 1;	// continuous
+		} else {
+			// semi / grenade / blade / ray: tap at a deliberate rate, not machine-gun fast
+			float ph = cltime - floor(cltime / 0.25) * 0.25;
+			if (ph < 0.05)
+				input_buttons = input_buttons | 1;
+		}
+	}
+
+	// release shot for snipers + rockets ("aim, then let go"); a quick tap fires from the hip
+	if (touch_ads_was && !adsfire_held && touch_sniper_armed) {
+		touch_sniper_fire_until = cltime + 0.10;
+		touch_sniper_armed = FALSE;
+	}
+	if (cltime < touch_sniper_fire_until)
+		input_buttons = input_buttons | 129;	// attack (1) + ADS (128) so the shot lands true
+
+	touch_ads_was = adsfire_held;
+
+	// look: rotate the *actual* client viewangles (the camera). modifying only
+	// input_angles turns the outgoing command but leaves the camera frozen, which
+	// also makes movement run relative to a different angle than you can see.
+	// sensitivity drops while aiming, and drops further through a sniper scope.
+	if (touch_look_dyaw || touch_look_dpitch) {
+		float lookmul = 1;
+		if (wz == 1)      lookmul = 0.55;	// ADS
+		else if (wz == 2) lookmul = 0.35;	// scoped sniper
+		if (getstatf(STAT_FACINGENEMY))		// aim-assist slowdown when on a target
+			lookmul = lookmul * 0.5;
+		vector va = getproperty(VF_CL_VIEWANGLES);
+		va_y = va_y + touch_look_dyaw * lookmul;
+		va_x = va_x + touch_look_dpitch * lookmul;
+		if (va_x >  89) va_x =  89;
+		if (va_x < -89) va_x = -89;
+		setproperty(VF_CL_VIEWANGLES, va);
+		input_angles_x = va_x;	// keep this frame's command in step with the view
+		input_angles_y = va_y;
+		touch_look_dyaw = 0;
+		touch_look_dpitch = 0;
+	}
+
+	// movement
+	if (touch_move_on) {
+		input_movevalues_x = input_movevalues_x + touch_move_fwd;
+		input_movevalues_y = input_movevalues_y + touch_move_side;
+	}
+};
+
+void() Touch_DrawStick =
+{
+	float i, finger = -1;
+	for (i = 0; i < TOUCH_MAXFINGERS; i = i + 1)
+		if (touch_active[i] && touch_role[i] == TR_MOVE) { finger = i; break; }
+
+	// fixed (locked) base bottom-left; knob rests at centre, rides the rim while held
+	float scx = g_width * 0.15, scy = g_height * 0.74;
+	float base = g_height * 0.34, knob = g_height * 0.15;
+	float kx = scx, ky = scy, ba = 0.16, ka = 0.22;
+	if (finger >= 0) {
+		kx = touch_curx[finger];
+		ky = touch_cury[finger];
+		ba = 0.30;
+		ka = 0.45;
+	}
+	drawpic([scx - base * 0.5, scy - base * 0.5, 0], "gfx/touch/stick_base", [base, base], [1, 1, 1], ba);
+	drawpic([kx - knob * 0.5, ky - knob * 0.5, 0], "gfx/touch/stick_knob", [knob, knob], [1, 1, 1], ka);
+};
+
+void() Touch_DrawControls =
+{
+	if (current_menu != MENU_NONE)
+		return;
+	if (last_input_was_gamepad)	// controller in use -- hide the on-screen pad
+		return;
+
+	Touch_DrawStick();
+
+	float i;
+	for (i = 0; i < TB_COUNT; i = i + 1) {
+		vector bg = Touch_BtnGeom(i);
+		float lit = Touch_BtnHeld(i);
+		drawpic([bg_x - bg_z * 0.5, bg_y - bg_z * 0.5, 0], Touch_BtnPic(i), [bg_z, bg_z], [1, 1, 1], lit ? 0.75 : 0.40);
+	}
+};
diff --git a/source/menu/m_menu.qc b/source/menu/m_menu.qc
index 91dc9fc..c667570 100644
--- a/source/menu/m_menu.qc
+++ b/source/menu/m_menu.qc
@@ -806,6 +806,18 @@ void(string id, vector pos, vector size, __inout vector scrollofs, float num_ser
 	int map_display_x = 0;
 	int player_display_x = 0;
 
+	//Keep the gamepad-selected row on screen: if the focused element is one of our rows, scroll to it.
+	string hovprefix = strcat(id, "scrl");
+	if (substring(_hover_actions[0], 0, strlen(hovprefix)) == hovprefix)
+	{
+		float sel = stof(substring(_hover_actions[0], strlen(hovprefix), -1));
+		float seltop = sel * rowsize.y;
+		if (seltop < scrollofs.y)
+			scrollofs.y = seltop;
+		else if (seltop + rowsize.y > scrollofs.y + size.y)
+			scrollofs.y = seltop + rowsize.y - size.y;
+	}
+
 	sui_list_view_begin(strcat(id, "scrl"), pos, size, rowsize, num_servers, scrollofs, [0, 16]);
 	for (float index = sui_list_item(listitem_pos); index > -1; index = sui_list_item(listitem_pos))
 	{
diff --git a/source/menu/main.qc b/source/menu/main.qc
index e9c007e..1c44344 100644
--- a/source/menu/main.qc
+++ b/source/menu/main.qc
@@ -300,6 +300,27 @@ void(float wantmode) m_toggle =
 	}
 };
 
+// Go up one menu level (for the controller B / "back" button). NZ:P has no engine-level
+// back-one-level key, so map each menu to its parent here.
+void() Menu_GoBack =
+{
+	Menu_PlaySound(MENU_SND_ENTER);
+	if (current_menu == MENU_VIDEO || current_menu == MENU_AUDIO || current_menu == MENU_CONTROL || current_menu == MENU_ACCESSIBILITY)
+		current_menu = MENU_OPTIONS;
+	else if (current_menu == MENU_GAMEPAD || current_menu == MENU_BINDINGS)
+		current_menu = MENU_CONTROL;
+	else if (current_menu == MENU_GAMESETTINGS)
+		current_menu = MENU_LOBBY;
+	else if (current_menu == MENU_COOPJOIN || current_menu == MENU_COOPCREATE)
+		current_menu = MENU_COOP;
+	else if (current_menu == MENU_COOPBROWSE || current_menu == MENU_COOPDIRECT)
+		current_menu = MENU_COOPJOIN;
+	else if (current_menu == MENU_PAUSE)
+		m_close();					// resume the game
+	else if (current_menu != MENU_MAIN)
+		current_menu = MENU_MAIN;	// any other submenu falls back to the main menu
+};
+
 float(float evtype, float scanx, float chary, float devid) Menu_InputEvent =
 {
 	last_input_deviceid = devid;
@@ -325,8 +346,15 @@ float(float evtype, float scanx, float chary, float devid) Menu_InputEvent =
 		return TRUE;
 	}
 
+	// Controller B = back one menu level (unless we're mid key-rebind).
+	if (evtype == IE_KEYDOWN && scanx == K_GP_B && _sui_binding_command == "")
+	{
+		Menu_GoBack();
+		return TRUE;
+	}
+
 	float used = sui_input_event(evtype, scanx, chary, devid);
-	
+
 	if (evtype == IE_KEYDOWN && !used)
 	{
 		string cmd = getkeybind(scanx);
diff --git a/source/menu/menu_coop.qc b/source/menu/menu_coop.qc
index def4893..aa104b9 100644
--- a/source/menu/menu_coop.qc
+++ b/source/menu/menu_coop.qc
@@ -147,7 +147,7 @@ void() Menu_Coop_Browse =
 	if (!sui_is_hovered("cbm_refresh") && !sui_is_hovered("cbm_back"))
 	{
 		sui_set_align([SUI_ALIGN_CENTER, SUI_ALIGN_END]);
-		sui_text([0, -30], MENU_TEXT_MEDIUM, "NOTE: Keyboard or gamepad is not supported for server selection.", [1, 1, 1], 1, 0);
+		sui_text([0, -30], MENU_TEXT_MEDIUM, "Select a server and press confirm (or tap) to connect.", [1, 1, 1], 1, 0);
 	}
 
 	sui_pop_frame();
@@ -312,7 +312,23 @@ string(string prev_id) fn ## _GetNextButton =	\
 	return ret;									\
 };
 
-NEXTBUTTON_FUNC(Menu_Coop_Browse, menu_coop_browse_buttons)
+//Browse needs a custom traversal: step through each server row (cbm_serverlistscrl<i>) so the
+//controller can pick a server, then the Refresh/Back buttons. Reusing the row ids means the
+//existing hover-highlight and click-to-connect paths work for the gamepad for free.
+string(string prev_id) Menu_Coop_Browse_GetNextButton =
+{
+	float n = num_cached_servers;
+	if (prev_id == "")
+		return (n > 0) ? "cbm_serverlistscrl0" : "cbm_refresh";
+	if (substring(prev_id, 0, 18) == "cbm_serverlistscrl")
+	{
+		float idx = stof(substring(prev_id, 18, -1));
+		return (idx + 1 < n) ? strcat("cbm_serverlistscrl", ftos(idx + 1)) : "cbm_refresh";
+	}
+	if (prev_id == "cbm_refresh")
+		return "cbm_back";
+	return (n > 0) ? "cbm_serverlistscrl0" : "cbm_refresh";	//cbm_back (or unknown) -> wrap to top
+};
 NEXTBUTTON_FUNC(Menu_Coop_Direct, menu_coop_direct_buttons)
 NEXTBUTTON_FUNC(Menu_Coop_Join, menu_coop_join_buttons)
 NEXTBUTTON_FUNC(Menu_Coop_Create, menu_coop_create_buttons)
@@ -341,7 +357,22 @@ string(string next_id) fn ## _GetPreviousButton =	\
 	return ret;										\
 };
 
-PREVBUTTON_FUNC(Menu_Coop_Browse, menu_coop_browse_buttons)
+string(string next_id) Menu_Coop_Browse_GetPreviousButton =
+{
+	float n = num_cached_servers;
+	if (next_id == "")
+		return "cbm_back";
+	if (substring(next_id, 0, 18) == "cbm_serverlistscrl")
+	{
+		float idx = stof(substring(next_id, 18, -1));
+		return (idx - 1 >= 0) ? strcat("cbm_serverlistscrl", ftos(idx - 1)) : "cbm_back";	//above first server -> wrap to back
+	}
+	if (next_id == "cbm_back")
+		return "cbm_refresh";
+	if (next_id == "cbm_refresh")
+		return (n > 0) ? strcat("cbm_serverlistscrl", ftos(n - 1)) : "cbm_back";
+	return "cbm_back";
+};
 PREVBUTTON_FUNC(Menu_Coop_Direct, menu_coop_direct_buttons)
 PREVBUTTON_FUNC(Menu_Coop_Join, menu_coop_join_buttons)
 PREVBUTTON_FUNC(Menu_Coop_Create, menu_coop_create_buttons)
diff --git a/source/menu/menu_vide.qc b/source/menu/menu_vide.qc
index add610e..491ede3 100644
--- a/source/menu/menu_vide.qc
+++ b/source/menu/menu_vide.qc
@@ -1,4 +1,4 @@
-string menu_video_buttons[12] = {"vm_resol", "vm_fulls", "vm_shfps", "vm_maxps", "vm_vsync", "vm_fov", "vm_gamma", "vm_uwide", "vm_parts", "vm_filtr", "vm_apply", "vm_back"};
+string menu_video_buttons[13] = {"vm_resol", "vm_fulls", "vm_shfps", "vm_maxps", "vm_vsync", "vm_fov", "vm_gamma", "vm_uwide", "vm_parts", "vm_filtr", "vm_rendr", "vm_apply", "vm_back"};
 
 float menu_video_init;
 float menu_video_modes;
@@ -10,6 +10,7 @@ float current_vsync;
 float current_ultrawide;
 float current_particles;
 float current_filter;
+float current_renderer;	// 0 = Vulkan, 1 = OpenGL
 
 #define MAX_VIDEO_MODES     128 // FIXME: arbitrary.
 
@@ -84,6 +85,7 @@ void() Menu_Video_StoreCurrentSettings =
     current_ultrawide = cvar("vid_ultrawide_limiter");
     current_particles = cvar("nzp_particles");
     current_filter = (substring(strtoupper(cvar_string("gl_texturemode")), 0, 10) == "GL_NEAREST");
+    current_renderer = (cvar_string("vid_renderer") == "gl" || cvar_string("vid_renderer") == "egl");
 }
 
 void() Menu_Video_Init =
@@ -144,6 +146,13 @@ void() Menu_Video_UpdateVsync =
     cvar_set("vid_vsync", ftos(current_vsync));
 };
 
+void() Menu_Video_UpdateRenderer =
+{
+    Menu_PlaySound(MENU_SND_ENTER);
+    current_renderer = !current_renderer;
+    cvar_set("vid_renderer", current_renderer ? "gl" : "vk");
+};
+
 void() Menu_Video_UpdateFiltering =
 {
     Menu_PlaySound(MENU_SND_ENTER);
@@ -287,6 +296,13 @@ void() Menu_Video =
     else filt_string = "LINEAR";
     Menu_DrawOptionValue(10, filt_string);
 
+    // Renderer (Graphics API)
+    Menu_Button(11, "vm_rendr", "RENDERER", "Graphics API - applies on restart. Vulkan recommended; OpenGL is the legacy fallback.") ? Menu_Video_UpdateRenderer() : 0;
+    string rend_string = "";
+    if (current_renderer) rend_string = "OPENGL";
+    else rend_string = "VULKAN";
+    Menu_DrawOptionValue(11, rend_string);
+
     Menu_DrawDivider(12.25);
     Menu_Button(-2, "vm_apply", "APPLY", "Save & Apply Settings.") ? Menu_Video_ApplySettings() : 0;
     Menu_Button(-1, "vm_back", "BACK", "Return to Configuration Menu.") ? current_menu = MENU_OPTIONS : 0;
diff --git a/source/menu/sui_sys.qc b/source/menu/sui_sys.qc
index 6be6baf..1c9b92b 100644
--- a/source/menu/sui_sys.qc
+++ b/source/menu/sui_sys.qc
@@ -871,12 +871,12 @@ float(float evtype, float scanx, float chary, float devid) sui_input_event =
 			_sui_mouse1_down();
 			return TRUE;
 		}
-		else if (scanx == K_DOWNARROW || scanx == K_GP_DPAD_DOWN)
+		else if (scanx == K_DOWNARROW || scanx == K_GP_DPAD_DOWN || scanx == K_GP_LTHUMB_DOWN)
 		{
 			_sui_menukey_downarrow();
 			return TRUE;
 		}
-		else if (scanx == K_UPARROW || scanx == K_GP_DPAD_UP)
+		else if (scanx == K_UPARROW || scanx == K_GP_DPAD_UP || scanx == K_GP_LTHUMB_UP)
 		{
 			_sui_menukey_uparrow();
 			return TRUE;
@@ -886,12 +886,12 @@ float(float evtype, float scanx, float chary, float devid) sui_input_event =
 			_sui_menukey_enter();
 			return TRUE;
 		}
-		else if (scanx == K_LEFTARROW || scanx == K_GP_DPAD_LEFT)
+		else if (scanx == K_LEFTARROW || scanx == K_GP_DPAD_LEFT || scanx == K_GP_LTHUMB_LEFT)
 		{
 			_sui_menukey_leftarrow();
 			return TRUE;
 		}
-		else if (scanx == K_RIGHTARROW || scanx == K_GP_DPAD_RIGHT)
+		else if (scanx == K_RIGHTARROW || scanx == K_GP_DPAD_RIGHT || scanx == K_GP_LTHUMB_RIGHT)
 		{
 			_sui_menukey_rightarrow();
 			return TRUE;
@@ -939,7 +939,7 @@ void(float width, float height) sui_begin =
 {
 	_action_elements_index = 0;
 	_sui_draw_initialized = TRUE;
-	
+
 	sui_reset_frame();
 	sui_push_frame([0, 0], [width, height]);
 }
